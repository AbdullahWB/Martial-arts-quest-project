import useAuth from "./useAuth"

const useQuery = () => {
    // const { user } = useAuth()
    // const { isLoading, isError, data, error } = useQuery({
    //     queryKey: ['query', user?.email],
    //     queryFn: async () => {
    //         const response = await fetch(`http://localhost:5000/c`)
    //     }
    // })
}